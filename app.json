{
  "name": "Doven",
  "success_url": "/admin",
  "env": {
    "SYMFONY_ENV": "prod",
    "APP_ENV": "prod",
    "APP_SECRET": {"generator": "secret"}
  },
  "addons": [
    "jawsdb"
  ],
  "buildpacks": [
    {
      "url": "heroku/php"
    },
    {
      "url": "heroku/nodejs"
    }
  ],
  "scripts": {
    "postdeploy": "php bin/console doctrine:schema:update --force && php bin/console user:add Doven panda contact@doven.fr --kaio && yarn run encore production --config assets/themes/acme/webpack.config.js"
  }
}